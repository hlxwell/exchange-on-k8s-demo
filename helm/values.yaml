auth-service:
  replicaCount: 1
  enabled: true

account-service:
  replicaCount: 1
  enabled: true

order-service:
  replicaCount: 1
  enabled: true

trade-service:
  replicaCount: 1
  enabled: true

e2e-test:
  enabled: true

db-migrator:
  enabled: true

postgresql:
  enabled: true
  postgresqlUsername: postgres
  postgresqlPassword: postgres
  pgHbaConfiguration: |-
    local all all trust
    host all all localhost trust
